// Swiper options for one page layout
var swiper = new Swiper('.swiper-container', {
	direction: 'vertical',
    slidesPerView: auto,
    freeMode: true,
    scrollbar: {
        el: '.swiper-scrollbar',
    },
    
    mousewheel: true,
});

