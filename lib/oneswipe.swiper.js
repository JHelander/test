// Swiper options for one page layout
var swiper = new Swiper('.swiper-container', {
	direction: 'vertical',
    slidesPerView: auto,
    autoHeight: true,
    freeMode: true,
    scrollbar: {
        el: '.swiper-scrollbar',
    },
    
    mousewheel: true,
});

